// string interpolation : it allows to directly embed expressions in the string . 

let firstName = "sneha" ;
let lastName = "arora" ;
let fullName = `${firstName} ${lastName}`; 
console.log(fullName);  // sneha arora

let age = 18 ; 
let msg = `I am ${age} years old .`
console.log(msg);

